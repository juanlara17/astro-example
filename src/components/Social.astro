---
const { platform, username, textColor } = Astro.props;
---

<a class={`text-${textColor}-500 hover:text-${textColor}-700 py-2 px-4 bg-purple-500 rounded rounded-xl no-underline`} href={`https://www.${platform}.com/${username}`} target="_blank">{platform}</a>